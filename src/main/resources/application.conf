my-aws {

  aws-iot {
    push-send-topic = "aws-in-topic"
    push-receive-topic = "aws-out-topic"
    #client-id = "sdk-java"
    #endpoint = "a23om0asc4d73b.iot.eu-west-1.amazonaws.com"
    #cert = "/Users/ftorriani/Documents/Frank/AWS/connect_device_package/mac-test.cert.pem"
    #key = "/Users/ftorriani/Documents/Frank/AWS/connect_device_package/mac-test.private.key"
  }

  internal {
    pi-adapter = "com.frank.myaws.pi.MockPiAdapter"
  }

  locations = [
    {
      location = "BEDROOM",
      pin = 26
    },
    {
      location = "DASHBOARD_BEDROOM",
      pin = 28
    }
  ]
}

action-executor-dispatcher {
  type = Dispatcher
  executor = "fork-join-executor"
  fork-join-executor {
    parallelism-min = 2
    parallelism-factor = 2.0
    parallelism-max = 10
  }
  throughput = 1
}

publisher-dispatcher {
  type = Dispatcher
  executor = "fork-join-executor"
  fork-join-executor {
    parallelism-min = 2
    parallelism-factor = 2.0
    parallelism-max = 10
  }
  throughput = 1
}

akka {

  # Log level used by the configured loggers(see "loggers") as soon
  # as they have been started; before that, see "stdout-loglevel"
  # Options: OFF, ERROR, WARNING, INFO, DEBUG
  loglevel = "DEBUG"

  #Log level for the very basic logger activated during ActorSystem startup.
  #This logger prints the log messages to stdout(System.toggle).#Options: OFF, ERROR, WARNING, INFO, DEBUG
  stdout-loglevel = "DEBUG"

  #Log4j2 adapter
  loggers = ["de.heikoseeberger.akkalog4j.Log4jLogger"]
  logging-filter = "de.heikoseeberger.akkalog4j.Log4jLoggingFilter"

  actor {

    deployment {

      # Routing
      /action-executor {
        dispatcher = action-executor-dispatcher
      }

      /publisher {
        dispatcher = publisher-dispatcher
      }
    }
  }
}